CREATE TABLE Students (
    student_id INT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE,
    age INT CHECK (age >= 17),
    course_id INT
);

CREATE TABLE Courses (
    course_id INT PRIMARY KEY,
    course_name VARCHAR(50) NOT NULL
);

ALTER TABLE Students
ADD CONSTRAINT fk_course
FOREIGN KEY (course_id) REFERENCES Courses(course_id);

INSERT INTO Courses VALUES (101, 'Computer Science');
INSERT INTO Courses VALUES (102, 'Mechanical');

INSERT INTO Students VALUES (1, 'Amit', 'amit@gmail.com', 18, 101);
INSERT INTO Students VALUES (2, 'Riya', 'riya@gmail.com', 19, 102);


-- Add new column
ALTER TABLE Students ADD phone VARCHAR(15);

-- Modify column (syntax may vary by DB)
ALTER TABLE Students MODIFY name VARCHAR(100);

-- Drop column
ALTER TABLE Students DROP COLUMN phone;

RENAME TABLE Students TO Student_Details;

TRUNCATE TABLE Student_Details;

DROP TABLE Student_Details;
DROP TABLE Courses;
