CREATE TABLE Student (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    marks INT
);

INSERT INTO Student VALUES (1, 'Amit', 80);
INSERT INTO Student VALUES (2, 'Riya', 90);

SELECT * FROM Student;

UPDATE Student
SET marks = 85
WHERE id = 1;

DELETE FROM Student
WHERE id = 2;

CREATE USER 'user1' IDENTIFIED BY '1234';

GRANT SELECT, INSERT ON Student TO 'user1';

REVOKE INSERT ON Student FROM 'user1'@'localhost';

START TRANSACTION;

INSERT INTO Student VALUES (3, 'Rahul', 70);

SAVEPOINT sp1;

INSERT INTO Student VALUES (4, 'Neha', 60);

ROLLBACK TO sp1;

COMMIT;
